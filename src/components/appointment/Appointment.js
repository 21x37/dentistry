import React from 'react';
import Header from '../header/Header';

const Appointment = () => {
    return (
        <div>
            <Header />
            <p>Appointment</p>
        </div>
    );
};

export default Appointment;